{
  "name": "capsign-subgraph",
  "version": "0.2.21",
  "description": "CapSign Protocol Subgraph for Base (Goldsky)",
  "scripts": {
    "codegen:sepolia": "graph codegen subgraph.base-sepolia.yaml",
    "codegen:mainnet": "graph codegen subgraph.base.yaml",
    "build:sepolia": "graph build subgraph.base-sepolia.yaml",
    "build:mainnet": "graph build subgraph.base.yaml",
    "deploy:sepolia": "./scripts/deploy.sh base-sepolia",
    "deploy:mainnet": "./scripts/deploy.sh base"
  },
  "devDependencies": {
    "@graphprotocol/graph-cli": "^0.56.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  }
}

